{
  "name": "@besto/harness",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "env:up": "docker-compose up -d",
    "env:down": "docker-compose down",
    "server:dev": "go run server/cmd/main.go",
    "web:dev": "webpack serve"
  }
}
